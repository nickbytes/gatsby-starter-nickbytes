import styled from 'styled-components'

export const Headline = styled.h1`
  font-size: 15px;
  line-height: 18px;
  font-weight: bold;
  margin: 0;
`

export const Sub = styled.h2`
  font-size: 15px;
  line-height: 18px;
  font-weight: normal;
  margin: 0 0 22px;

  &:last-of-type {
    margin-bottom: 0;
  }
`
